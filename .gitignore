# Datos del servidor generados en produccion; nunca versionar ni desplegar.
data.json
data.json.tmp
.dianery-admin-token.php
.dianery-db.php

# Carpetas locales de asistentes/agentes. No forman parte del artefacto publico.
.claude/
.codex/
.agents/

node_modules/
*.log
.thumbnail

# Imágenes subidas por el admin (runtime): viven en /uploads del servidor, no en git.
/uploads/*

# Artefacto de build local (lo genera tools/build-local.sh; se regenera).
dist/
